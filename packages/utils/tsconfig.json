{
	"comment": "Holds the Paraglide i18n output (generated by the root `i18n:compile` script). Purely generated JS + .d.ts, so no type-check script — consumers get types via the emitted declarations.",
	"compilerOptions": {
		"target": "ES2022",
		"module": "ES2022",
		"moduleResolution": "Bundler",
		"strict": true,
		"skipLibCheck": true,
		"allowJs": true,
		"checkJs": false,
		"noEmit": true,
		"types": []
	},
	"include": ["paraglide/**/*"]
}
